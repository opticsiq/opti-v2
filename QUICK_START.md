# دليل التشغيل السريع - Opti

## 🚀 تشغيل التطبيق

### الطريقة الأسهل (مستحسنة):
```bash
./run-servers.sh
```

### الطريقة اليدوية:
```bash
# 1. تشغيل الخادم الخلفي
cd server
node index.js &

# 2. تشغيل الخادم الأمامي (في terminal منفصل)
cd ..
npm run dev
```

## 🔗 الروابط

بعد تشغيل الخوادم، ستكون الروابط التالية متاحة:

- **الصفحة الرئيسية:** http://localhost:8080
- **صفحة اختبار الاتصال:** http://localhost:8080/test
- **تسجيل دخول العملاء:** http://localhost:8080/login
- **تسجيل دخول المدراء:** http://localhost:8080/admin/login
- **API الخلفي:** http://localhost:5000

## 👥 حسابات الاختبار

### حساب العميل:
- **البريد:** customer@test.com
- **كلمة المرور:** password123

### حساب المدير:
- **البريد:** admin@test.com
- **كلمة المرور:** admin123

## 🧪 اختبار الاتصال

إذا واجهت مشاكل في الاتصال:

1. **اذهب إلى:** http://localhost:8080/test
2. **اضغط على:** "تشغيل جميع الاختبارات"
3. **تأكد من:** ظهور علامات ✅ خضراء

## ❗ حل المشاكل الشائعة

### إذا لم يفتح الرابط:
```bash
# تأكد من أن الخوادم تعمل
curl http://localhost:8080
curl http://localhost:5000/api/health

# إذا لم تعمل، أعد تشغيل الخوادم
pkill -f "vite"
pkill -f "node.*index.js"
./run-servers.sh
```

### إذا ظهرت رسالة "تعذر الاتصال بالخادم":
1. تأكد من تشغيل الخادم الخلفي على المنفذ 5000
2. تأكد من تشغيل الخادم الأمامي على المنفذ 8080
3. استخدم صفحة الاختبار للتشخيص

### إذا ظهرت أخطاء في console:
1. افتح Developer Tools (F12)
2. تحقق من تبويب Network للأخطاء
3. تحقق من تبويب Console للرسائل

## 📱 استخدام التطبيق

### للعملاء:
1. اذهب إلى http://localhost:8080/login
2. سجل دخول بـ customer@test.com / password123
3. ستنتقل لصفحة الطلبات تلقائياً

### للمدراء:
1. اذهب إلى http://localhost:8080/admin/login
2. سجل دخول بـ admin@test.com / admin123
3. ستنتقل للوحة التحكم تلقائياً

## 🔧 إيقاف الخوادم

لإيقاف جميع الخوادم:
```bash
# إذا كنت تستخدم السكريبت
Ctrl+C

# أو يدوياً
pkill -f "vite"
pkill -f "node.*index.js"
```

---

**نصيحة:** استخدم السكريبت `./run-servers.sh` دائماً لتجنب مشاكل تشغيل الخوادم!